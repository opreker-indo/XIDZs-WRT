# packages/

Place `.apk` packages here for the OpenWrt 25.12 Image Builder.

The Image Builder auto-indexes this directory. Most packages are installed
from fantastic-packages feeds (configured in PATCH.sh). Only packages not
available in any feed need to be placed here manually.
